package Utilidades;

public class ruta {
    public String origen = System.getProperty("user.dir");
    public String separador = System.getProperty("file.separator");
    public String rutaImagen = origen + separador + "logo" + separador;
}
